export default function style() {
  return (
    {
      grid: {
        display: "flex",
        justifyContent: "center"
      },
      container: {
        display: "flex",
        justifyContent: "center"
      }
    }
  );
}
